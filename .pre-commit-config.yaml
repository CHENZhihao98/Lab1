repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Detect hardcoded secrets
        language: system
        entry: gitleaks protect --staged --redact --verbose
        pass_filenames: false
