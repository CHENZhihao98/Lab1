services:
  app:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - path: .env
        required: false
    command: >
      sh -c "pip install -r requirements.txt -q && python app.py"
    ports:
      - "5000:5000"
